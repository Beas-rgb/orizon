# Teste grátis. Segredos não entram aqui: preencha no painel do Render.
services:
  - type: web
    name: orizon-api
    runtime: docker
    plan: free
    branch: master
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: production
      - key: DB_POOL_SIZE
        value: "2"
      - key: DB_MAX_OVERFLOW
        value: "0"
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: APP_PUBLIC_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
      # E-mail (Mailtrap preferido). Preencha no painel do Render — nunca no git.
      - key: MAILTRAP_API_TOKEN
        sync: false
      - key: MAILTRAP_FROM_EMAIL
        sync: false
      - key: MAILTRAP_FROM_NAME
        value: Horizon
      # Conta TI / painel /app/dev.html — só hash no banco; valores no Render.
      - key: ADMIN_NOME
        sync: false
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_SENHA
        sync: false
