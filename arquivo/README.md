# Arquivo

Código parado de propósito. Não usar em deploy nem em desenvolvimento novo.

## frontend/

App React + Vite antiga. O Horizon segue só com `web/` (HTML/JS puro servido
em `/app`).

Não reinstale dependências aqui. Se precisar consultar algo, abra os arquivos;
não rode `npm run dev` desta pasta no fluxo normal.
